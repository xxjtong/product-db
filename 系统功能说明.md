# 产品数据库 — 系统功能说明

## 项目概述

物联网产品中心数据库，支持产品选型对比、规格书生成、方案设计、报价单生成。

**技术栈**: FastAPI + SQLAlchemy 2.0 + Pydantic | Vue 3 + TypeScript + Vite | SQLite（WAL）| systemd（生产运行方式）

**本地环境**:
- 后端 API: http://localhost:8000 (Swagger: /docs)
- 前端: http://localhost:5173
- 账号: admin / admin (DEV_MODE 自动创建)

---

## 架构与数据流总览（2026-09 全量梳理）

### 业务域与模块构成

| 业务域 | 数据表 | 路由模块 |
|--------|--------|---------|
| 产品主数据 | `products` + 5 张映射表 + `product_images` + `product_categories`（裸多对多）+ `product_files` | `products`、`product_import`、`product_files` |
| 品类与字典 | `device_categories`、`category_spec_definitions`、5 张字典表、`suppliers` | `categories`、`dictionaries`、`suppliers` |
| 方案与报价 | `solutions`、`solution_items`、`quotations`、`quotation_items`、`product_dependencies` | `solutions`、`quotations` |
| BOM 编辑器与快照 | `bom_templates`、`solution_bom_snapshots` | `bom_templates` |
| AI 与 Agent | `ai_conversations`、`ai_messages`、`ai_usage_logs` | `ai`、`agent` |
| 用户与治理 | `users`、`login_logs`、`download_logs`、`field_settings`、`system_settings` | `auth`、`admin`、`system_settings` |

### 核心业务链路

**1) 产品录入：三条路汇入同一套主数据**

| 路径 | 入口 | 经过 | 落库 |
|------|------|------|------|
| 手工 | `ProductFormView` | `POST /products` → `validate_specs` → `write_mappings` | `products` + 5 映射表 + `product_images` + `product_categories` |
| AI 提取 | `AiExtractCard` | `POST /products/ai-fetch[-file]` → DeepSeek / 视觉模型（无 key 回落正则）→ 预览确认 | 同上 |
| Excel 导入 | `ImportView` | `import-preview`（解析 + 列映射）→ `import-confirm`（品类匹配 → 整批校验 → **按名称+型号去重** → 落库） | `products` + 新厂商 |

**2) 方案 → 报价单**

1. `POST /solutions/{id}/items` 加条目 → `_recalc_totals` 重算 `total_cost` / `total_price`
2. `GET /solutions/{id}/suggest` 依 `product_dependencies.depends_on_category_id` 推荐缺失产品
3. `POST /quotations`（可带 `solution_id`，可带 `extra_items` 一并并入方案）复制方案条目，并为每条写入 `product_snapshot`（JSON 副本，含成本）。**R71 起创建报价单只有一份实现**：`services/quotation_service.py`（REST / AI 工具 / Hermes 共用）
4. 导出 `GET /quotations/{id}/export-xlsx`：按 `cost_visible` 决定是否含成本列，写 `download_logs` 并自增 `download_count`

**3) BOM 编辑器与快照（双向）**

| 动作 | 端点 | 说明 |
|------|------|------|
| 读 | `GET /solutions/{id}/bom-snapshot` | **只读**：无快照时按模板 + 条目在内存生成，不落库（R56 起） |
| 写 | `PUT /solutions/{id}/bom-snapshot` | `_keep_server_cost`（无成本权限者不改成本列）→ `_sync_snapshot_to_items` 回写 `solution_items` |
| 导出 | `GET /solutions/{id}/bom-snapshot/export-xlsx` | 10 列布局，成本固定在 J 列 |

列映射：`C`=SKU、`E`=数量、`F`=单价、`G`=折扣%、`I`=备注；数据行从第 3 行（快照）或第 5 行（兜底生成）起。

**4) 规格书与 AI**

- 规格书：`GET /products/{id}/spec-sheet` → `build_spec_html` → weasyprint 子进程 → PDF（失败回落 HTML）。**依赖服务器系统字体**，缺字体时会静默丢中文。
- AI 对话：`POST /ai/chat` SSE → `run_agent` → DeepSeek；无 key 走 mock；会话与用量分别写 `ai_conversations` / `ai_messages` / `ai_usage_logs`。
- Hermes Agent：`POST /agent/chat` **丢弃客户端传来的 system**，由服务端从 `system_settings.agent_prompt` 读取并替换 `{{TOKEN}}`（用调用方自己的 JWT）等占位符后注入（R60）；`model` 由服务端固定、messages 上限 200 条。转发到 `127.0.0.1:8642` 后不做工具执行，审批卡片只由 `"测试审批"` 自测钩子触发（**限 admin**，普通用户说这句话只当普通聊天），写操作实际靠 prompt 软约束 + Hermes 自身权限。回复结束后前端调 `POST /agent/suggestions`（**走本项目 DeepSeek，不走 Hermes**，否则每轮多背 15.8k 系统提示词）拿 3 条追问按钮。

### 数据交换

| 方向 | 对端 | 协议与说明 |
|------|------|-----------|
| 出站 | DeepSeek API | HTTPS/JSON，key 取 `AI_GATEWAY_KEY` |
| 出站 | Hermes agent | HTTP/SSE（默认 `127.0.0.1:8642`） |
| 出站 | 小米视觉模型 | HTTPS，仅图片 OCR 提取 |
| 出站 | ipapi.co | HTTPS，仅当本地 ip2region 离线库未命中 |
| 出站 | 任意公网 URL | 产品抓取 / 图片下载，经 `validate_url`（SSRF 防护） |
| 本地 | SQLite（WAL） | `backend/product_db.db`；备份用 `sqlite3 .backup` + `integrity_check` |
| 本地 | `backend/app/uploads/` | 图片与附件；UUID 命名 + 魔数校验 + 大小上限 |
| 本地 | `frontend/dist/` | 由后端直接托管（无独立静态服务） |

### 权限模型

| 层 | 规则 |
|----|------|
| 读 | 任意登录用户，列表走 `filter_by_ownership`。`/categories` 与 `/categories/tree` **同口径**（产品列表的品类筛选器读的是树）；树会把「父不可见、子可见」的节点提升为根级，否则整棵子树会消失 |
| 写 | `check_ownership(strict=True)` |
| 管理 | 字典/品类/供应商/BOM 模板的写操作与 `/admin/*` 一律 `require_admin` |
| 成本价 | 三层判定：admin 恒可见 → `users.can_view_cost`（三态覆盖）→ 全局 `field_settings.cost_price` |
| 用户可控 URL | 只允许 http/https（前端会直接打开，防空协议注入） |
| 匿名可达接口限流 | 统一走 `app/services/rate_limit.py`。登录用**失败尝试计数**（DB：`login_logs`，按 IP + 窗口，默认 10 次/300 秒，失败与「已禁用账户」都计数）；注册用**请求频次计数**（进程内滑动窗口，按 IP，默认 5 次/3600 秒，不分成败——要挡的是批量建号）。注册成功**不写** `login_logs`（那是登录审计）。⚠️ 频次计数是进程内实现，只适用单进程部署 |
| AI/Agent 业务范围 | 四个提示词（`ai_system_prompt` / `ai_keyword_prompt` / `ai_extract_prompt` / `agent_prompt`）统一口径（R67）：圈定业务 → 越界**一句话**拒绝并给可做示例（不解释不展开）→ 必须查库/查数据、不猜测不编造 → 直给结论（不复述问题、不寒暄、不解释过程、不提工具名称，不输出无关科普与免责声明）。其中 `agent_prompt` 另含「不输出非本用户的记忆或内部信息」，被问「你记得什么」时拒绝（R66）。提示词在后台可改，代码里的 `_PROMPT_DEFAULTS` 是唯一来源并与库内值同步。但 Hermes 自身有 shell/browser 能力，**能力**边界仍需在 Hermes 侧收窄 |
| Agent 长期记忆 ⚠️ | Hermes 的长期记忆（`MEMORY.md`/`USER.md`）是**按 profile 一份文件**（`$HERMES_HOME/memories`），**未按用户隔离**：所有产品库用户与飞书/CLI 入口共用同一份，且每轮都注入系统提示词。`X-Hermes-Session-Key` 只标识会话身份，**不能**分区记忆（R65 结论，见 AGENTS.md）。R66 已加提示词护栏挡住直接询问，但那是**软约束**；结构性隔离未做 —— 仍不要主动把敏感内容交给 agent 去"记住" |

### 数据模型分组

| 分组 | 表数 | 关键表与关系 |
|------|------|-------------|
| 产品主数据 | 1 | `products`：→ 品类 RESTRICT；厂商/供应商 SET NULL |
| 映射与图片 | 7 | 5 张映射表（通讯/协议/供电/接口/传感）+ `product_images` + `product_categories`（均 CASCADE 于产品） |
| 品类与规格 | 2 | `device_categories`（自引用）、`category_spec_definitions` |
| 字典与供应商 | 6 | 5 张字典表 + `suppliers` |
| 方案 | 2 | `solutions`、`solution_items`（items CASCADE、product RESTRICT） |
| 报价单 | 2 | `quotations`、`quotation_items`（`solution_id` SET NULL；条目为 JSON 快照） |
| BOM 与快照 | 2 | `bom_templates`、`solution_bom_snapshots`（Univer JSON 快照） |
| AI | 3 | `ai_conversations`、`ai_messages`（CASCADE）、`ai_usage_logs` |
| 用户与审计 | 3 | `users`、`login_logs`、`download_logs` |
| 配置 | 2 | `system_settings`、`field_settings` |
| 产品文件 | 1 | `product_files`（CASCADE，含外链） |

> **三处「快照型」设计**：`quotation_items.product_snapshot`、`bom_templates.snapshot`、
> `solution_bom_snapshots.snapshot` 都是 JSON 副本 —— 历史单据不随产品改价变动，但一旦生成
> 就与产品主数据脱钩（例如快照里没有成本列时不会自动回填）。

---

## 1. 后端 API (14 路由模块)

| 模块 | 端点 | 核心功能 |
|------|------|---------|
| `/api/products` | 18 | CRUD + AI识别 + 规格书HTML/PDF + 对比 + Excel导入导出 + 拼音搜索 |
| `/api/categories` | 10 | 多层级树 + 规格定义管理 + 级联删除 |
| `/api/solutions` | 10 | 方案BOM管理 + 完整性检查 + 缺失推荐 (已移除BOM编辑器/依赖检查UI按钮) |
| `/api/quotations` | 12 | 报价CRUD + snapshot + xlsx导出 + BOM编辑 (GET/PUT /bom) + 下载计数 |
| `/api/bom-templates` | 6 | BOM模板CRUD + 快照保存/加载/xlsx导出 (模板保存UI已移除) |
| `/api/ai` | 4 | SSE流式对话 + LLM产品匹配 + Tool Calling(4工具，**写工具仅在带方案上下文时下发**) + 对话持久化 + Mock回退 + 用量统计(含入口来源) |
| `/api/auth` | 8 | JWT登录 + 自注册 + 频率限制 + 用户管理 |
| `/api/admin` | 10 | 用户CRUD + 密码重置 + 登录/下载日志 + 字段可见性 + AI Prompt + AI用量 |
| `/api/dicts` | 20 | 通讯方式/协议/供电/传感指标/厂商 CRUD |
| `/api/suppliers` | 4 | 供应商CRUD |
| `/api/settings` | 3 | 系统设置KV管理 |
| `/api/product_files` | 6 | 产品文件上传/下载/删除/外链 (下载需auth，外链限 http/https) |
| `/api/agent` | 10 | Hermes agent 代理（服务端注入 system；不做工具执行/审批拦截）+ 文件上传 + 提示词/配置 + 快捷答复建议 + 主动停止标记 + 自测审批钩子(限admin) |

> 统一前缀是 `/product-db/api`（上表简写为 `/api`），例如 `/api/products` 实为 `/product-db/api/products`。
> `product_import` 与 `products` 共用前缀，故 15 个路由文件中 `include_router` 为 14 条。

---

## 2. 前端页面 (16 视图)

| 页面 | 路由 | 功能 |
|------|------|------|
| 登录 | `/login` | 登录/自注册切换 |
| 产品列表 | `/products` | 品类+厂商+通讯/协议/供电筛选 + 多选对比 + Excel导出导入 |
| 产品详情 | `/products/:id` | 图片 + 5类映射 + 规格表 + 变体 + 依赖 + 文件管理 |
| 产品编辑 | `/products/:id/edit` | 动态表单 + AI预填 + 图片管理 |
| 产品对比 | `/products/compare` | 多产品差异矩阵 |
| 产品导入 | `/products/import` | Excel预览 + 列映射 + 确认导入（重复行按「名称+型号」跳过并提示条数） |
| 品类管理 | `/categories` | 树形表格 + 规格定义CRUD |
| 字典管理 | `/dictionaries` | 6标签切换(通讯/协议/供电/传感/厂商/供应商) + CRUD |
| 方案列表 | `/solutions` | 搜索+筛选 + 行内状态下拉 + 批量选择 + ¥格式 |
| 方案详情 | `/solutions/:id` | 客户信息 + AI方案助手(GenUI) + 产品清单(行内编辑,功能描述含全部规格参数) + 生成报价单 |
| 报价列表 | `/quotations` | 搜索+筛选 + 行内状态下拉 + 批量选择 + ¥格式 |
| 报价详情 | `/quotations/:id` | 明细表格(功能描述含全部规格参数) + 合计行 + **BOM编辑器** + xlsx导出 + 下载计数 |
| 供应商 | `/suppliers` | 供应商 CRUD（联系人字段 `contact_person`） |
| Agent 对话 | `/agent` | Hermes agent 对话 + 内联审批卡片 + 文件链接 + 模型生成的快捷答复按钮 |
| 管理面板 | `/admin` | 用户/字段可见性/AI Prompt/注册控制/AI用量/日志 |

> 品类管理视图（`CategoriesView.vue`）仍存在，但路由 `/categories` 已重定向到 `/dictionaries`（字典页内含品类标签）。

---

## 3. AI 方案助手

### 匹配流程 (2026-06 重构)

```
用户输入 → Round 0: LLM(deepseek-chat) 从 db_ctx 匹配产品ID（**首轮**给的 db_ctx 含全部产品；之后各轮只给词表，产品交给 search_products 工具）
         → JSON {keywords, matches: {关键词: [产品ID]}}
         → 代码验证ID → DB取真实数据 → 去重交替 → 展示
         → DSML fallback: 解析原生tool-call格式 → SQL LIKE
         → Round 1: Chat LLM(deepseek-v4-flash) 流式文本回复
```

### 匹配规则
- 每关键词 top 10 → 跨关键词去重 → 每关键词最多 5 条 → 交错合并
- 价格/排序过滤由 SQL 层处理
- 语义理解: "地下车库"→光照/防尘, "无线"→WiFi/LoRaWAN
- DB Context: **分两档**（R73）—— 会话首轮 `full`（全部产品[ID:xxx]描述+specs，约 25k+ tokens，用于冷启动选型）；之后各轮 `compact`（只有品类/厂商/通讯/协议/供电/传感指标词表 + 提示改用工具检索）。两档各自 300s TTL 缓存

### 工具 (4个)
| 工具 | 功能 |
|------|------|
| `search_products` | 关键词+品类+通讯+协议+供电搜索, SQL LIKE 回退 |
| `get_product_detail` | 完整产品详情 |
| `list_categories` | 品类列表 |
| `create_quotation` | 从方案创建报价单 |

### GenUI 动态组件
| 组件 | 触发条件 | 功能 |
|------|---------|------|
| `SolutionProductCard` | 搜索到产品 | 勾选 + 数量 + 加入方案 + 对比 |
| `QuoteDraftCard` | AI 请求生成报价单 | **预览**卡（条目/合计/将并入方案的产品）+「确认生成」按钮；点确认才调 `POST /quotations` 落库 |

### SSE 事件流
```
connect → tool(可见状态) → products → component(GenUI) → text(逐字) → done → quick_replies
```

---

## 4. 方案 (Solution) 完整流程

```
① 客户信息 ─────────────────────────────────────
   客户名称 / 项目名称 / 状态 / 备注 / 总价

② AI 方案助手 ──────────────────────────────────
   "10个温湿度传感器 + 1个LoRaWAN网关"
   → 🤖 AI推荐产品 (GenUI卡片)
   → ☑勾选产品 + 数量 → [加入方案(N)] [对比N]
   → tool 状态消息实时可见

③ 产品清单 (BOM) ────────────────────────────────
   [+ 批量选品] 弹窗搜索 + 多选 + 批量添加 (懒加载)
   列表: 产品/型号/功能描述/数量/单价/折扣%/小计/备注/删除
   行内编辑: 数量/折扣/备注直接改, 本地更新不重新加载

④ 生成报价单 ───────────────────────────────────
   [生成报价单] 按钮 或 AI 对话 → 都要经 POST /quotations（services/quotation_service
   是唯一实现）；AI 那条先出预览卡，点「确认生成」才落库
   报价单页可编辑BOM表格 (HTML模式, 添加/删除行)
```

---

## 5. 报价单 BOM 编辑器

- HTML 表格内联编辑, 列: `# | 产品名称 | 型号/SKU | 功能描述 | 数量 | 单价 | 折扣% | 小计 | 备注`
- 按钮: 重新加载/保存/添加行/导出xlsx
- 每行垃圾桶删除 + confirm确认
- 保存回写 `quotation_items` 表 (数据源: 报价单自身)
- XLSX 导出含合计行, 列格式匹配编辑器
- 通知统一用全局 toast (非 alert)

---

## 6. 权限系统 (2026-06 新增)

- **8 张表**加 `created_by` → `users.id`: products, categories, manufacturers, suppliers, 4 dict tables
- `filter_by_ownership()`: 列表查询过滤 (admin 全量, 普通用户 NULL/自己/admin(id=1))
- `check_ownership()`: 单资源 PUT/DELETE 403 校验
- SECRET_KEY 启动校验 ≥32 字符 (DEV_MODE 除外)

---

## 7. 列布局统一

所有表格统一为:

```
# | 产品名称 | 型号/SKU | 功能描述 | 数量 | 单价 | 折扣% | 小计 | 备注
```

- 产品清单 (方案详情页)
- BOM 表格 (报价单编辑器)
- 报价单详情页 (只读表格)
- 三处 XLSX 导出 (方案BOM / 报价单 / 报价BOM)
- 价格统一 ¥ + toLocaleString() 千位分隔

---

## 8. 组件清单 (共 14 个)

| 组件 | 说明 |
|------|------|
| **AiChat** | 浮动AI对话面板 (可拖拽/缩放, DOMPurify清洗) |
| **AiExtractCard** | AI 智能录入卡片 (文本/URL/图片 OCR 提取产品信息) |
| **AiUsageStats** | AI 用量统计面板 (总次数/Token/操作类型) |
| **AsyncContainer** | 异步加载容器 (loading/error/retry 统一) |
| **BOMSpreadsheet** | HTML表格编辑器 (支持solutionId/quotationId, 添加/删除行, toast) |
| **SolutionProductCard** | GenUI — AI推荐产品卡片 (勾选+数量+加入方案) |
| **QuoteDraftCard** | GenUI — 报价单**预览**卡（确认后才落库，落库走公共模块） |
| **ProductFiles** | 产品文件上传/下载/预览 |
| **DependencyEditor** | 产品依赖管理 |
| **DependencyGraph** | Canvas 依赖关系图 |
| PageHeader | 页面标题栏 + 操作按钮 (items-center对齐) |
| Pagination | 分页组件 |
| SearchInput | 搜索输入 (IME安全 + 防抖) |
| Modal | 通用弹窗 |
| ConfirmDialog | 确认对话框 |
| TagBadge | 标签徽章 |

---

## 9. 数据模型核心表

**字典层 (5)**: manufacturers, dict_comm_methods, dict_comm_protocols, dict_power_supplies, dict_sensor_metrics
(均含 `created_by`)

**核心层 (3)**: device_categories, category_spec_definitions, products
(products.category_id → `ondelete=RESTRICT`)

**映射层 (6)**: product_comm_methods, product_comm_protocols, product_power_supplies, product_hardware_interfaces, product_sensor_capabilities, product_images

**多对多 (1)**: product_categories (无ORM模型, 统一通过 `product_category_helper.py` 操作)

**业务层 (8)**: suppliers, solutions, solution_items, quotations, quotation_items, product_dependencies, bom_templates, solution_bom_snapshots

**系统层 (9)**: users, login_logs, system_settings, field_settings, ai_conversations, ai_messages, ai_usage_logs, download_logs, product_files

> **外键策略（R57 起强制生效）**：SQLite 默认不校验外键，`app/database.py` 在 engine
> `connect` 事件里逐连接打开 `PRAGMA foreign_keys=ON`，此后模型/迁移里的 `ondelete` 才起作用。
> 三类策略：`CASCADE`（子行脱离父行没意义：映射表、条目、消息、中间表）／
> `SET NULL`（审计与归属：各类 `created_by`、日志 `user_id`、`manufacturer_id`/`supplier_id`/
> `parent_id`/`solution_id`）／`RESTRICT`（`products.category_id`、`solution_items.product_id`、
> `quotation_items.product_id`）。详见 `docs/database.md`「外键与级联」。

---

## 10. 当前数据

- 396 产品 / 50 品类 / 48 厂商 / 55 供应商
- 字典: 18 通讯 + 17 协议 + 11 供电 + 36 传感指标
- 方案: 7 / 报价单: 8 / AI 对话: 18
- 测试: pytest **623 通过**（+1 skipped） + vitest **98 通过** + Playwright E2E **7 个 spec 套件**（生产可跑 4 个，见 README）, vue-tsc 0 errors

> 数据为**生产库**（product-db.cn）实测值，统计于 2026-09-21。测试数随迭代变化，以实际运行为准。
> 本机跑测试/迁移请用 `backend/venv/bin/python`（3.9，依赖齐）；仓库里另有一个 `.venv`（3.14）是缺依赖的遗留环境。

## 11. 文档

- `docs/architecture.md` — 完整架构总览 (技术栈/分层/路由/组件/AI/安全)
- `docs/database.md` — 数据库设计 (34 表/ER/索引/JSON 策略/权限模型/外键与级联)

---

## 12. 变更日志

### R17 (2026-06-18)

**功能:**
- 方案页/报价单"功能描述"列 = 描述 + 全部规格参数（`|` 分隔）
- 自动剥离描述中产品 URL
- HTML 单行截断，Excel 完整内容

**安全:**
- passlib → bcrypt 直接调用
- Excel 图片嵌入 SSRF 防护
- Agent 工具移除 cost_price 泄露（2处）

**测试:**
- Playwright E2E 框架 + 核心流程测试

**优化:**
- 删除 Univer 死代码（897行 + 18包）
- flattenTree 去重
- token URL 编码
